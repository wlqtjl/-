server {
    listen 80;
    server_name _;  # 替换为你的域名，如: server_name wozai.example.com;

    client_max_body_size 2m;

    # 安全头（与应用层互补）
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "DENY" always;

    # 访问日志
    access_log /var/log/nginx/wozai_access.log;
    error_log  /var/log/nginx/wozai_error.log;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        proxy_connect_timeout 10s;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
        proxy_buffering off;
    }

    # 静态文件缓存（CSS/JS/字体）
    location ~* \.(css|js|woff2?|ttf|eot|svg|ico)$ {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        expires 7d;
        add_header Cache-Control "public, immutable";
    }
}

# ======== HTTPS 配置（可选，使用 certbot 自动签发）========
# 取消下方注释并替换域名后运行:
#   sudo certbot --nginx -d wozai.example.com
#
# server {
#     listen 443 ssl http2;
#     server_name wozai.example.com;
#
#     ssl_certificate     /etc/letsencrypt/live/wozai.example.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/wozai.example.com/privkey.pem;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#     ssl_ciphers         HIGH:!aNULL:!MD5;
#
#     client_max_body_size 2m;
#
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header X-Frame-Options "DENY" always;
#
#     location / {
#         proxy_pass http://127.0.0.1:8080;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_connect_timeout 10s;
#         proxy_read_timeout 60s;
#         proxy_send_timeout 60s;
#     }
# }
#
# server {
#     listen 80;
#     server_name wozai.example.com;
#     return 301 https://$host$request_uri;
# }
